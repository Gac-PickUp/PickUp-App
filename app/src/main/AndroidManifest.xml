<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PickUp"
        tools:targetApi="31">


        <activity
            android:name=".ViewGameActivity"
            android:exported="true" />

        <activity android:name="com.example.pickup.ClickGameItem">
            <!-- Optionally, you can add intent filters or other activity properties here -->
        </activity>

        <activity
            android:name=".PasswordResetActivity"
            android:exported="true"
            android:label="@string/title_activity_password_reset"
            android:theme="@style/Theme.PickUp"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".SignUpActivity"
            android:exported="true"
            android:label="@string/title_activity_sign_up_page"
            android:theme="@style/Theme.PickUp"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".LandingPage"
            android:exported="true"
            android:theme="@style/Theme.PickUp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginPage"
            android:exported="true"
            android:label="@string/title_activity_login_page"
            android:theme="@style/Theme.PickUp"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="MainActivity"
            android:theme="@style/Base.Theme.PickUp" />
        <activity
            android:name=".CreateGameActivity"
            android:exported="true"
            android:label="CreateGame"
            android:theme="@style/Base.Theme.PickUp"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>